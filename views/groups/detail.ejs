<!DOCTYPE html>
<html lang="en">
    
    <%- include('../partials/head', {variant:'/css/groups.css'}) %>

<body>
       
    <main>
        <% if(group) { %>
            <%- include('group', { group }) %>
        <% } else { %>
            <h2>No encontramos ese grupo</h2>
        <% } %>
    </main>

</body>
</html>
